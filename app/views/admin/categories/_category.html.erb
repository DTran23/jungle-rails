<tr>
  <td>
    <%= category.name %>
  </td>
  <td>
  <%= link_to fa_icon('trash'), admin_category_path(category.id), class: 'btn btn-sm btn-danger', method: :delete, data: { confirm: "Are you sure?" } %>
</td>
</tr>